name: Go

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]


jobs:
  build:
    strategy:
      matrix:
        go-version: [1.19.x]
        os: [ubuntu-latest, macos-latest]
    env:
      CGO_ENABLED: 0
    runs-on: ${{ matrix.os }}
    steps:
    - name: Set up Go
      uses: actions/setup-go
      with:
        go-version: ${{ matrix.go-version }}

    - name: Checkout code
      uses: actions/checkout

    - name: build
      run: go build ./...
    
    - name: vet
      run: go vet ./...
